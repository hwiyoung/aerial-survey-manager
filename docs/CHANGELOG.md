# 변경 이력

## v2.0.0-rc.1 (2026-07-16)

> 릴리즈 후보입니다. `v1.x` 대비 처리 엔진, 배포 환경변수, 스토리지 경로,
> 인증·조직 공유 정책과 DB 스키마가 변경되어 업그레이드 전 백업과
> 마이그레이션 확인이 필요합니다.

| # | 항목 | 내용 |
|---|------|------|
| REFACTOR | 조직 공동 운영 모델 | 관리자/일반 사용자 구분과 사용자·조직·권한 관리 API/UI 제거, 프로젝트·그룹·사용자 정의 카메라 모델을 조직 공유로 통일 |
| SECURITY | 프로젝트 산출물 보호 | 썸네일·정사영상·TiTiler 직접 접근 차단, 단기 서명 URL과 동일 출처 MinIO URL 적용 |
| RELIABILITY | Celery 복구 | GPU 작업 redelivery 복구, 썸네일/삭제 작업 late ack, 삭제 잔여 파일 검증 및 재시도 |
| DEPLOY | 배포 구조 단순화 | 미사용 celery-beat 제거, 설치 폴더 기준 상대경로 및 조직 공동 운영 계정 안내 통일 |
| CLEANUP | Airflow 레거시 제거 | 미사용 Airflow Compose·DAG·의존성·문서를 제거하고 Metashape 실행 스크립트를 Celery 전용 `pipeline`으로 분리 |
| REFACTOR | 처리 단계 정의 통합 | API·Celery 엔진·상태 계산의 단계 이름·순서·메시지를 단일 정의로 통합하고 포인트클라우드 진행 표시 누락 수정 |
| REFACTOR | 재시작 판정 분리 | 처리 API에 섞여 있던 체크포인트 유효성·재시작 요약 로직을 독립 서비스와 단위 테스트로 분리 |
| DEPENDENCY | 미사용 패키지 제거 | 배포되지 않던 변화탐지 AI 스크립트와 관련 지오스페이셜 패키지, 프론트 미사용 패키지를 제거하고 Pillow·GDAL 버전을 고정 |
| CLEANUP | 처리 엔진 단일화 | 비활성 ODM·외부 API 엔진 구현과 큐·웹훅·환경 설정을 제거하고 Metashape GPU 큐로 실행 경로를 통일 |
| CLEANUP | 미참조 파일 정리 | 추적되던 Celery Beat 상태 파일, 사용되지 않는 대시보드 컴포넌트와 Metashape 보조 스크립트를 제거하고 관련 미사용 import를 정리 |
| FIX | 최초 계정 안내 명확화 | 설치용 `ADMIN_*` 값은 신규 DB에만 적용되고 기존 DB는 기존 로그인 정보를 유지한다는 안내를 설치 화면과 배포 문서에 명시 |

---

## 2026-03-27

| # | 항목 | 내용 |
|---|------|------|
| FIX | 프론트엔드 권역 목록 통일 | "제주 권역" 제거 (DB/백엔드에 없는 권역), 새 폴더 placeholder "경기권역" → "수도권북부" 수정. 전체 시스템 8개 권역 통일 |

---

## Sprint 5 (2026-03-12 ~ 03-19)

| # | 항목 | 내용 |
|---|------|------|
| F2 | 로컬 경로 이미지 불러오기 | HTTP 업로드 → 로컬 경로 등록 방식. 폴더 경로 입력 시 DB에 경로만 등록, 처리 시 symlink 참조 |
| F3 | 업로드 완료 시 즉시 처리 예약 | "완료 후 처리" 옵션으로 야간 무인 운영 지원 |
| F5 | 도엽 단위 클립+머지 | 1:5,000 / 1:1,000 도엽 단위 클립. bounds-only 메모리 캐시(~14MB) |
| F6 | 내보내기 후 COG 삭제 옵션 | 다운로드 완료 후 COG 삭제 선택, 삭제 전 썸네일 자동 생성 |
| F7 | 베이스맵 on/off 토글 | 대시보드/처리화면 베이스맵 레이어 토글 (localStorage 동기화) |
| F8 | 처리화면 지도 자유 이동 | FitBounds 1회 제한 + "원래 범위로" 복귀 버튼 |
| F9 | 처리화면 정보 통합 | 싱글/더블클릭 뷰 통합, COG 삭제 UI 개선 |
| F10 | 안정성 + 단일 계정 전환 | Celery 이중 실행 방지, 처리 단계 표시, 회원가입 제거 |
| F11 | 썸네일 전용 워커 분리 | thumbnail 큐 분리, GDAL 우선 생성 (22초 → 0.1~5초) |

**추가 변경:** Stuck job 복구, WebSocket 죽은 연결 정리, Healthcheck 강화, DB 인덱스 추가, 파일 스캔 비동기화, EO 포인트 토글, 호스트 파일시스템 마운트, 최초 계정 자동 생성, 라이선스 자동 활성화

---

## Sprint 1~4 (2026-01-22 ~ 02-14)

| Sprint | 목표 | 결과 |
|--------|------|------|
| 1차 | 사용자/조직/권한 API + UI | 완료 — users/organizations/permissions 라우트, 관리자 메뉴 조건 노출 |
| 2차 | 조직 격리 + 쿼터 + 감사 로그 | 완료 — 조직 스코프 필터, 쿼터 정책, 감사 이벤트 |
| 3차 | 배치 API + 부분 실패 제어 | 완료 — `POST /projects/batch`, 실패 항목 재시도 UX |
| 4차 | 엔진 정책 + COG 성능 + 모니터링 | 완료 — GPU 처리 단일 엔진, COG 캐시 최적화, SLO 로깅 |

**보완 대상:** 다운로드 토큰 스코프 정책 통일, 그룹 삭제 정합성 점검, owner/조직 정책 충돌 경로

---

## 주요 변경 이력

| 날짜 | 내용 |
|------|------|
| 2026-02-26 | 업로드 성능 튜닝 (partSize 32MB, concurrency 3) |
| 2026-02-13 | 로컬/MinIO 스토리지 추상화, 워커 아키텍처 분리, 외부 COG 삽입 |
| 2026-02-12 | 저장소 최적화 (result.tif 업로드 제거, 원본 이미지 삭제 기능) |
| 2026-02-10 | COG 중복 변환 제거, 오프라인 타일 캐시, 권역 폴백 수정 |
| 2026-02-08 | 처리 로그 개선, 중간산출물 .work/ 폴더 분리 |
| 2026-02-06 | 개발/배포 이미지 분리, 코드 보호 (.pyc) |
| 2026-02-04 | 출력 좌표계 자동 설정, COG 원본 GSD 유지 |
| 2026-02-03 | 썸네일 시스템, 오프라인 지도, 라이센스 관리 |
| 2026-02-02 | 멀티 프로젝트 업로드, 글로벌 업로드 상태 |
| 2026-01-27 | GPU 처리 엔진 Celery 통합, GPU 가속 |
| 2026-01-22 | TiTiler 통합 (메모리 90% 절감) |

*마지막 업데이트: 2026-07-16*
