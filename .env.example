# Aerial Survey Manager
# Environment variables for Docker Compose

# Database
POSTGRES_PASSWORD=your-secure-password

# JWT Authentication
JWT_SECRET_KEY=your-super-secret-jwt-key-change-in-production

# MinIO Storage
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=your-secure-minio-password
# CRITICAL: S3 Multipart Upload에서 presigned URL 생성에 사용됩니다.
# 브라우저에서 접속하는 nginx 주소와 동일하게 설정해야 합니다 (same-origin 필수)
# 예: 브라우저가 http://192.168.10.203:8081 로 접속한다면 MINIO_PUBLIC_ENDPOINT=192.168.10.203:8081
MINIO_PUBLIC_ENDPOINT=

# --- Storage Paths ---
# IMPORTANT: Set these to a drive with sufficient free space (1TB+ recommended)
# MinIO will refuse uploads with HTTP 507 if disk space falls below 10%

# Processing data path on host (mapped to /data/processing in containers)
# Example: /mnt/fastdisk/aerial-data/processing
PROCESSING_DATA_PATH=

# MinIO data path on host (mapped to /data in minio container)
# Example: /mnt/fastdisk/minio-data
MINIO_DATA_PATH=

# --- Processing Engines ---
# ODM (Default, Docker-in-Docker)
# EXTERNAL_ENGINE_URL=http://your-external-engine:5000

# Metashape Engine (Requires GPU & License)
METASHAPE_LICENSE_KEY=

# Processing data path on host (mapped to /data/processing in containers)
# Example: /mnt/fastdisk/aerial-data/processing
PROCESSING_DATA_PATH=
